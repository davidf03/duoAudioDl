{
  "devDependencies": {
    "concurrently": "^5.3.0",
    "parcel-bundler": "^1.12.4",
    "parcel-plugin-svelte": "^4.0.6",
    "parcel-plugin-web-extension": "^1.6.1",
    "typescript": "^4.0.2",
    "web-ext": "^5.0.0"
  },
  "scripts": {
    "start": "concurrently \"parcel src/manifest.json\" \"web-ext run --source-dir ./dist/ -t firefox-desktop --start-url https://www.duolingo.com/ \"",
    "build": "parcel build src/manifest.json --no-source-maps"
  },
  "dependencies": {
    "svelte": "^3.20.1"
  }
}
