<script>
import { queue } from '../../store';
import CardList from '../Cards/CardList.svelte'
</script>

<div class="aud-c-home aud-o-block">
  <!-- {#if $queue.length > 0}
    <CardList cardlist={$queue} pending/>
  {/if}
  {#if $queue.length === 0}
    <p>No cards in queue</p>
  {/if} -->
  <p>queue</p>
</div>
